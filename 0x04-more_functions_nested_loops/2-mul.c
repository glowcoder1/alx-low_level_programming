#include "main.h"

/**
 * mul - multiplies two integers
 * Description: returns the result of multiplying 2 integers
 * Return: the result an int
 * @a : a number
 * @b : a number
 */

int mul(int a, int b)
{
	return (a * b);
}
